public class TallCircular extends Figure{
    private final boolean isTall = true;
    private final boolean isCircular = true;

    public TallCircular(Board.FigureColor color, boolean hole) {
        super(color, hole);
    }
}
